/**
 * Функция с подготовленным контентом для запроса AI
 */
export const getAiMessageContent = (query: string, details: string) => {
  return `
Ты умный поиск по фильмам. У тебя есть список фильмов с названием и сюжетом: ${details}.

Ты получаешь пользовательский запрос: "${query}".

Найди только те фильмы, у которых title или plot максимально похожи на запрос.

ВАЖНО:
- Верни ответ **только в формате JSON массива строк**, например: ["Название1", "Название2"]
- Никаких пояснений, текста или других символов.
- Если ничего не найдено, верни пустой массив: []
- Ответ должен быть корректным JSON
  `;
};
